//TreeManager object which specifies the position of the trees in the game.
function TreeManager(){

    this.treeArray = [];
    
    this.treeArray.push(new Tree(450, 0));
    this.treeArray.push(new Tree(740, 0));
}